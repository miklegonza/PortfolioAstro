---
interface Props {
    name: string;
    color: string;
    logo: string;
}

const { name, color, logo } = Astro.props;
---

<img
    class="p-1"
    alt="Static Badge"
    src=`https://img.shields.io/badge/${name}-${color}?style=flat&logo=${logo}&logoColor=white`
/>
